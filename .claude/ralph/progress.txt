[2026-01-24] 0.1-project-init: COMPLETED
- Created ralph/ directory structure with subdirectories: core, mixins, proxies, collectors, triggers, cli
- Created __init__.py files in all 7 directories (ralph/ and its 6 subdirectories)
- Each __init__.py includes a docstring describing the module's purpose
- Verification passed: `ls ralph/` shows all required directories
- Verification passed: `find ralph -name '__init__.py' | wc -l` returns 7
- Next priorities: 0.2-dependencies (install pyyaml), 1.1-config-classes (core configuration)

[2026-01-24] 1.1-config-classes: COMPLETED
- Created ralph/core/config.py with:
  - TriggerType enum: ONE_SHOT, STEP_BASED, PROBABILISTIC, PERIODIC
  - StrategyType enum: All strategy types from spec (~50 strategies covering L0-L3 layers)
  - TriggerConfig dataclass with should_trigger(current_step, rng) method
  - FaultConfig dataclass with id, strategy, trigger, parameters, enabled, severity, expected_behavior
- All trigger types tested: ONE_SHOT, STEP_BASED, PERIODIC working correctly
- Verification passed: imports work correctly
- This unblocks: 1.2-registry, 1.3-scheduler, 3.1-base-proxy, 5.2-yaml-config-loader
- Next priorities: 0.2-dependencies (quick), 1.2-registry (ProxyRegistry), 2.x-mixins (strategy implementations)

[2026-01-24] 1.2-registry: COMPLETED
- Created ralph/core/registry.py with ProxyRegistry class
- Implemented class methods:
  - @register(target): Class decorator for registering proxy classes
  - get_proxy(target) -> Type[BaseProxy]: Retrieves proxy class for a target
  - get_supported_strategies(target) -> Set[StrategyType]: Gets supported strategies for a proxy
  - list_targets() -> List[str]: Lists all registered targets (sorted)
  - is_registered(target) -> bool: Checks if target has registered proxy
  - unregister(target): Removes a registration (for testing)
  - clear(): Clears all registrations (for testing)
  - get_all_registrations() -> Dict: Gets copy of all registrations
- Error handling: KeyError for missing targets, ValueError for duplicate registrations
- Updated ralph/core/__init__.py to export ProxyRegistry
- Verification passed: `python3 -c 'from ralph.core.registry import ProxyRegistry; print(ProxyRegistry.list_targets())'` returns []
- This unblocks: 3.1-base-proxy (which unblocks all proxy implementations)
- Next priorities: 3.1-base-proxy (critical path), 1.3-scheduler, 2.x-mixins, 3.2-dual-stream-collector

[2026-01-24] 1.3-scheduler: COMPLETED
- Created ralph/core/scheduler.py with TriggerScheduler class
- Implemented methods:
  - __init__(seed): Initialize with optional random seed for reproducibility
  - add_config(config): Add FaultConfig to scheduler (raises ValueError on duplicate)
  - remove_config(config_id): Remove config (raises KeyError if not found)
  - get_config(config_id): Retrieve config by ID
  - list_configs(): List all registered config IDs (sorted)
  - update_step(step): Update the current training step
  - get_step(): Get current step
  - should_trigger(config_id): Check if a specific fault should trigger
  - get_active_faults(): Get all faults that should trigger at current step
  - reset_one_shot(config_id): Reset a one-shot trigger to fire again
  - reset_all_one_shots(): Reset all one-shot triggers
  - clear(): Clear all configs and reset state
  - set_seed(seed): Set random seed for probabilistic triggers
- Trigger type support:
  - ONE_SHOT: Triggers exactly at specified step, only fires once (tracked in _triggered_one_shots)
  - STEP_BASED: Triggers within [start_step, end_step] range (inclusive)
  - PROBABILISTIC: Uses seeded RNG for reproducible probabilistic triggering
  - PERIODIC: Triggers when current_step % every_n_steps == 0
- Updated ralph/core/__init__.py to export TriggerScheduler
- Created tests/ralph/test_scheduler.py with comprehensive unit tests
- All trigger types verified working via manual testing
- Verification passed: `python3 -c 'from ralph.core.scheduler import TriggerScheduler; print("OK")'`
- This unblocks: No direct blockers, but scheduler is used by InjectionEngine
- Next priorities: 3.1-base-proxy (critical path), 2.x-mixins, 3.2-dual-stream-collector

[2026-01-24] 2.1-delay-mixin: COMPLETED
- Created ralph/mixins/delay.py with DelayMixin class
- Implemented methods:
  - _apply_delay(seconds: float): Sleeps for the specified duration (guards against negative values)
  - _strategy_delay(*args, **kwargs): Reads delay_seconds from self._config.parameters (defaults to 10.0), applies delay, then calls original function
- Updated ralph/mixins/__init__.py to export DelayMixin
- Created tests/ralph/test_delay_mixin.py with 12 comprehensive unit tests:
  - TestApplyDelay: Tests for positive, zero, negative, and fractional delay values
  - TestStrategyDelay: Tests for config usage, default values, argument passing, return values, exception propagation
  - TestDelayMixinIntegration: Tests for multiple inheritance and real timing
- All 12 tests pass
- Verification passed: `python3 -c 'from ralph.mixins.delay import DelayMixin; print("OK")'`
- This unblocks: 4.1-ray-get-proxy, 4.2-all-reduce-proxy, 4.3-reward-manager-proxy, 4.4-checkpoint-save-proxy, 9.3-generate-proxy, 12.1-tool-call-proxy
- Next priorities: 2.2-tensor-mixin, 2.3-exception-mixin, 3.1-base-proxy (critical path)

[2026-01-24] 2.2-tensor-mixin: COMPLETED
- Created ralph/mixins/tensor.py with TensorCorruptionMixin class
- Implemented methods:
  - _corrupt_tensor(tensor, noise_scale): Adds Gaussian noise (tensor + randn_like(tensor) * noise_scale)
  - _inject_nan(tensor, ratio): Sets random ratio of elements to NaN (clones tensor to avoid mutation)
  - _inject_inf(tensor, ratio, positive=True): Sets random ratio of elements to +/-Inf
  - _scale_tensor(tensor, scale): Returns tensor * scale
  - _corrupt_result_tensors(result, noise_scale): Recursively corrupts tensors in dict/list/tuple structures
  - _inject_nan_result(result, nan_ratio): Recursively injects NaN into nested structures
  - _inject_inf_result(result, inf_ratio, positive): Recursively injects Inf into nested structures
  - _strategy_corrupt_tensor(*args, **kwargs): Reads noise_scale from config (default 0.1), corrupts result tensors
  - _strategy_inject_nan(*args, **kwargs): Reads nan_ratio from config (default 0.001), injects NaN
  - _strategy_inject_inf(*args, **kwargs): Reads inf_ratio and positive from config, injects Inf
- Updated ralph/mixins/__init__.py to export TensorCorruptionMixin
- Created tests/ralph/test_tensor_mixin.py with 40 comprehensive unit tests:
  - TestCorruptTensor: Noise addition, shape preservation, dtype handling, zero/large noise scale
  - TestInjectNan: NaN injection, zero/full ratio, shape preservation, original tensor immutability
  - TestInjectInf: Positive/negative Inf injection, ratio handling
  - TestScaleTensor: Positive/negative/zero scaling
  - TestCorruptResultTensors: Single tensor, dict, list, tuple, nested structures, non-tensor passthrough
  - TestStrategyCurruptTensor: Config usage, default values, dict results
  - TestStrategyInjectNan: Config usage, default ratio, nested results
  - TestStrategyInjectInf: Config usage, positive/negative Inf from config
  - TestTensorCorruptionMixinIntegration: Multiple inheritance, device preservation
- All 40 tests pass
- Verification passed: `python -c 'from ralph.mixins.tensor import TensorCorruptionMixin; print("OK")'`
- This unblocks: 4.1-ray-get-proxy, 4.2-all-reduce-proxy, 4.3-reward-manager-proxy, 9.1-gae-proxy, 9.4-compute-values-proxy, 13.1-megatron-optimizer-proxy
- Next priorities: 2.3-exception-mixin, 2.4-skip-mixin, 2.5-result-mixin, 3.1-base-proxy (critical path)

[2026-01-24] 2.3-exception-mixin: COMPLETED
- Created ralph/mixins/exception.py with ExceptionMixin class
- Defined EXCEPTION_MAP dict mapping string names to exception classes:
  - IOError, RuntimeError, ValueError, PermissionError, TimeoutError, FileNotFoundError, OSError
  - Also added: KeyError, TypeError, AttributeError, ConnectionError, MemoryError (extra for flexibility)
- Implemented methods:
  - _raise_exception(exc_type, message, **kwargs): Raises specified exception type with message
    - Raises ValueError if exc_type not found in EXCEPTION_MAP
    - Accepts additional kwargs for future extensibility
  - _strategy_raise_exception(*args, **kwargs): Reads exc_type and message from self._config.parameters
    - exc_type is required (raises ValueError if missing)
    - message defaults to "Fault injection: {exc_type}"
    - Original function is never called (this is a terminal strategy)
- Updated ralph/mixins/__init__.py to export ExceptionMixin
- Created tests/ralph/test_exception_mixin.py with 31 comprehensive unit tests:
  - TestExceptionMap: Verifies all 7 required exception types are present
  - TestRaiseException: Tests each exception type, unknown type handling, message preservation
  - TestStrategyRaiseException: Config reading, default message, error cases, all exception types
  - TestExceptionMixinIntegration: Multiple inheritance, exact exception class verification
- All 31 tests pass
- Verification passed: `python -c 'from ralph.mixins.exception import ExceptionMixin; print("OK")'`
- This unblocks: 4.1-ray-get-proxy, 4.4-checkpoint-save-proxy, 12.1-tool-call-proxy
- Next priorities: 2.4-skip-mixin, 2.5-result-mixin, 3.1-base-proxy (critical path), 3.2-dual-stream-collector

[2026-01-24] 3.1-base-proxy: COMPLETED
- Created ralph/proxies/base.py with BaseProxy abstract class
- Implemented all required methods and attributes:
  - SUPPORTED_STRATEGIES: Set[StrategyType] = set() (class attribute, empty set default)
  - __init__(original_fn, collector): Stores original function and optional collector
  - _build_strategy_map(): Creates dict mapping StrategyType to _strategy_X methods
  - set_config(config): Validates strategy is in SUPPORTED_STRATEGIES, raises ValueError if not
  - set_step(step): Updates self._current_step
  - set_rng(rng): Sets random number generator for probabilistic triggers
  - __call__(*args, **kwargs): Checks _should_inject and dispatches to strategy or original
  - _should_inject(): Checks config exists, enabled, and trigger.should_trigger
  - _execute_strategy(*args, **kwargs): Looks up strategy method and calls it
  - _record_injection_start(): Calls collector.record_fault_injection, returns fault_id
  - _record_injection_end(fault_id, outcome, duration_ms): Calls collector.record_fault_outcome
  - _get_layer() -> str: Abstract method, must be implemented by subclasses
  - _get_target_name() -> str: Returns original function's __name__
  - Helper methods: get_original(), get_config(), get_step()
- Updated ralph/proxies/__init__.py to export BaseProxy
- Created tests/ralph/test_base_proxy.py with comprehensive unit tests (45+ tests covering all functionality)
- Verification passed: Attempting to instantiate BaseProxy raises TypeError with "abstract" message
- This unblocks: 4.1-ray-get-proxy, 4.2-all-reduce-proxy, 4.3-reward-manager-proxy, 4.4-checkpoint-save-proxy, 9.1-gae-proxy, 9.3-generate-proxy, 9.4-compute-values-proxy, 10.1-gpu-memory-injector, 11.1-data-proto-concat-proxy, 12.1-tool-call-proxy, 13.1-megatron-optimizer-proxy, 13.3-optimizer-step-proxy
- Next priorities: 2.4-skip-mixin, 2.5-result-mixin, 3.2-dual-stream-collector, then proxy implementations (4.x series)

[2026-01-24] 2.4-skip-mixin: COMPLETED
- Created ralph/mixins/skip.py with SkipMixin class
- Implemented methods:
  - _skip_operation(default_return=None): Returns default value without calling original function
  - _repeat_operation(times, *args, **kwargs): Calls original N times, returns last result
    - Guards against times < 1 (defaults to 1)
  - _strategy_skip(*args, **kwargs): Reads 'default_return' from config (defaults to None)
  - _strategy_repeat(*args, **kwargs): Reads 'times' from config (defaults to 2)
- Updated ralph/mixins/__init__.py to export SkipMixin
- Created tests/ralph/test_skip_mixin.py with 34 comprehensive unit tests:
  - TestSkipOperation: 7 tests for default None, custom defaults, dict/list, falsy values (0, '', False)
  - TestRepeatOperation: 8 tests for N calls, returns last result, 0/negative times, args passing, exceptions
  - TestStrategySkip: 5 tests for config usage, default None, complex defaults
  - TestStrategyRepeat: 5 tests for default 2 times, config times, arg passing
  - TestSkipMixinIntegration: 4 tests for multiple inheritance, skip+repeat sequence, side effects
- All tests verified passing via manual test execution
- Verification passed: `python3 -c 'from ralph.mixins.skip import SkipMixin; print("OK")'` (direct import)
- This unblocks: No direct blockers, but SkipMixin can be used by proxy implementations
- Next priorities: 2.5-result-mixin, 3.2-dual-stream-collector, then proxy implementations (4.x series)


[2026-01-24] 2.5-result-mixin: COMPLETED
- Created ralph/mixins/result.py with ResultModificationMixin class
- Implemented methods:
  - _modify_result(result, modifier_fn): Applies arbitrary modifier function to result
  - _negate_result(result): Negates numeric values (int, float, tensor), recursing for dict/list/tuple
    - Unsupported types (string, None, etc.) are returned unchanged
  - _set_constant(result, value): Sets all numeric values to constant
    - For tensors: Returns torch.full_like(result.float(), value).to(result.dtype)
    - For int/float: Returns type(result)(value) to preserve type
    - Handles nested structures recursively
  - _strategy_modify_result(*args, **kwargs): Reads 'modifier_fn' from config (optional)
  - _strategy_reward_flip(*args, **kwargs): Negates all numeric values in result
  - _strategy_constant_reward(*args, **kwargs): Reads 'constant_value' from config (defaults to 0.0)
- Updated ralph/mixins/__init__.py to export ResultModificationMixin
- Created tests/ralph/test_result_mixin.py with 52 comprehensive unit tests:
  - TestModifyResult: 5 tests for applying modifier functions
  - TestNegateResult: 14 tests for int, float, tensor, dict, list, tuple, nested structures
  - TestSetConstant: 12 tests for setting constant values, preserving tensor shapes
  - TestStrategyModifyResult: 4 tests for strategy implementation
  - TestStrategyRewardFlip: 6 tests for reward flip strategy
  - TestStrategyConstantReward: 7 tests for constant reward strategy
  - TestResultModificationMixinIntegration: 4 tests for multiple inheritance, sequences
- All 52 tests pass
- Verification passed: `python3 -c 'from ralph.mixins.result import ResultModificationMixin; print("OK")'`
- This unblocks: 4.3-reward-manager-proxy, 9.1-gae-proxy, 9.4-compute-values-proxy
- Next priorities: 3.2-dual-stream-collector, 4.1-ray-get-proxy, 4.2-all-reduce-proxy


[2026-01-24] 3.2-dual-stream-collector: COMPLETED
- Created ralph/collectors/dual_stream.py with DualStreamCollector class
- Implemented all required methods:
  - __init__(output_dir, telemetry_file, labels_file, buffer_size, auto_flush): Sets up paths and buffers
  - record_fault_injection(fault_type, target_layer, target_function, severity, parameters, expected_behavior) -> str: Returns unique fault_id
  - record_fault_outcome(fault_id, outcome, duration_ms): Appends outcome to labels
  - record_telemetry(event_type, data) -> str: Records system telemetry events
  - flush(): Writes buffered data to JSONL files
  - close(): Flushes and closes the collector
- Additional features implemented:
  - Thread-safe with threading.Lock for concurrent access
  - Auto-flush when buffer reaches configured size
  - Context manager support (__enter__, __exit__)
  - Active fault tracking (get_active_fault_count)
  - Buffer size monitoring (get_buffer_sizes)
  - Proper handling of None parameters and empty fault_ids
  - Unicode support in JSONL output
- Data format:
  - Labels JSONL has record_type: "fault_injection_start" or "fault_injection_end"
  - Telemetry JSONL has event_id, timestamp, event_type, data
- Updated ralph/collectors/__init__.py to export DualStreamCollector
- Created tests/ralph/test_dual_stream_collector.py with comprehensive tests (40+ test cases)
- Verification passed: All 10 manual tests pass, JSONL files are valid and parseable
- This unblocks: 5.1-injection-engine, 14.1-log-parser, 14.2-metrics-hook
- Next priorities: 4.1-ray-get-proxy, 4.2-all-reduce-proxy, 4.3-reward-manager-proxy, 4.4-checkpoint-save-proxy


[2026-01-24] 4.1-ray-get-proxy: COMPLETED
- Created ralph/proxies/l0_ray.py with RayGetProxy class
- Inherits from BaseProxy, DelayMixin, ExceptionMixin, TensorCorruptionMixin
- Set SUPPORTED_STRATEGIES = {DELAY, RAISE_EXCEPTION, CORRUPT_TENSOR, OBJECT_LOST, PARTIAL_FAILURE}
- Implemented _get_layer() returning 'L0'
- Implemented _strategy_object_lost(futures, timeout):
  - Raises ray.exceptions.ObjectLostError when Ray is installed
  - Falls back to custom ObjectLostError when Ray is not installed
  - Reports first ref in error message for list inputs
- Implemented _strategy_partial_failure(futures, timeout):
  - Calls original ray.get first to get results
  - Sets fail_ratio (default 0.5) of results to None
  - Handles both single ref and list of refs
  - Passes timeout and other kwargs to original
- Decorated with @ProxyRegistry.register('ray.get')
- Created ObjectLostError fallback exception class with object_ref, owner_address, call_site attributes
- Updated ralph/proxies/__init__.py to export RayGetProxy and ObjectLostError (with conditional import for torch)
- Created tests/ralph/test_l0_ray_proxy.py with comprehensive unit tests covering:
  - TestRayGetProxyRegistration: Registration and supported strategies verification
  - TestRayGetProxyBasics: _get_layer, call without config, disabled config
  - TestDelayStrategy: Config usage, default delay
  - TestRaiseExceptionStrategy: Various exception types
  - TestCorruptTensorStrategy: Single tensor, list, nested structures
  - TestObjectLostStrategy: Single ref, list refs, error attributes
  - TestPartialFailureStrategy: Single ref, list, default ratio, kwargs passing
  - TestRayGetProxyIntegration: Step-based triggering, periodic trigger, collector recording
- Verification passed: Code compiles without syntax errors
- This unblocks: 8.1-ray-put-proxy, 8.2-execute-all-proxy, 5.1-injection-engine (partial)
- Next priorities: 4.2-all-reduce-proxy, 4.3-reward-manager-proxy, 4.4-checkpoint-save-proxy



[2026-01-24] 4.2-all-reduce-proxy: COMPLETED
- Created ralph/proxies/l1_distributed.py with AllReduceProxy class
- Inherits from BaseProxy, DelayMixin, TensorCorruptionMixin
- Set SUPPORTED_STRATEGIES = {DELAY, CORRUPT_TENSOR, INJECT_NAN, INJECT_INF, DEADLOCK}
- Implemented _get_layer() returning 'L1'
- Implemented _get_current_rank() helper to get current process rank (returns 0 if not distributed)
- Implemented strategy methods (all modify tensor in-place before calling original):
  - _strategy_corrupt_tensor: Adds Gaussian noise to tensor before all_reduce
  - _strategy_inject_nan: Injects NaN values into tensor before all_reduce
  - _strategy_inject_inf: Injects +/-Inf values into tensor before all_reduce
  - _strategy_deadlock: Hangs specified rank in infinite sleep loop
- Config parameters:
  - CORRUPT_TENSOR: noise_scale (float, default 0.1)
  - INJECT_NAN: nan_ratio (float, default 0.001)
  - INJECT_INF: inf_ratio (float, default 0.001), positive (bool, default True)
  - DEADLOCK: deadlock_rank (int, required) - raises ValueError if missing
- Decorated with @ProxyRegistry.register('torch.distributed.all_reduce')
- Updated ralph/proxies/__init__.py to export AllReduceProxy (with conditional import)
- Created tests/ralph/test_l1_distributed_proxy.py with 27 comprehensive unit tests:
  - TestAllReduceProxyRegistration: Registration and supported strategies verification
  - TestAllReduceProxyBasics: _get_layer, call without config, disabled config
  - TestDelayStrategy: Config usage, default delay, kwargs passing
  - TestCorruptTensorStrategy: Modifies input, default noise scale, kwargs passing
  - TestInjectNanStrategy: NaN injection, default ratio, kwargs passing
  - TestInjectInfStrategy: Positive/negative Inf injection, default ratio, kwargs passing
  - TestDeadlockStrategy: Missing rank error, non-matching rank calls original, matching rank hangs
  - TestAllReduceProxyIntegration: Step-based triggering, periodic trigger, collector recording, rank detection
- All 27 tests pass
- This unblocks: 8.3-all-gather-proxy, 8.4-barrier-proxy, 5.1-injection-engine (partial)
- Next priorities: 4.3-reward-manager-proxy, 4.4-checkpoint-save-proxy, 5.1-injection-engine


[2026-01-24] 4.3-reward-manager-proxy: COMPLETED
- Created ralph/proxies/l2_verl.py with RewardManagerProxy class
- Inherits from BaseProxy, DelayMixin, TensorCorruptionMixin, ResultModificationMixin
- Set SUPPORTED_STRATEGIES = {DELAY, CORRUPT_TENSOR, REWARD_FLIP, CONSTANT_REWARD}
- Implemented _get_layer() returning 'L2'
- Implemented strategy methods:
  - _strategy_corrupt_tensor: Corrupts tensor data with Gaussian noise (reads noise_scale from config)
  - _strategy_reward_flip: Negates all reward values (positive becomes negative)
  - _strategy_constant_reward: Returns constant value for all rewards (reads constant_value from config)
  - DELAY: Inherited from DelayMixin, works out of the box
- Decorated with @ProxyRegistry.register('RewardManager.__call__')
- Updated ralph/proxies/__init__.py to export RewardManagerProxy
- Created tests/ralph/test_l2_verl_proxy.py with 31 comprehensive unit tests:
  - TestRewardManagerProxyRegistration: Registration and supported strategies verification
  - TestRewardManagerProxyBasics: _get_layer, call without config, disabled config, unsupported strategy
  - TestDelayStrategy: Config usage, default delay, kwargs passing
  - TestCorruptTensorStrategy: Single tensor, multiple tensors, default noise scale, preserves non-tensors
  - TestRewardFlipStrategy: Single tensor, multiple tensors, nested structures, non-numeric preservation
  - TestConstantRewardStrategy: Single tensor, default zero, negative value, multiple tensors, shape preservation, dtype
  - TestRewardManagerProxyIntegration: Step-based trigger, periodic trigger, collector recording, exception recording
- All 31 tests pass
- Verification passed: `from ralph.proxies import RewardManagerProxy` works correctly
- This unblocks: 5.1-injection-engine (partial), 10.3-update-actor-proxy
- Next priorities: 4.4-checkpoint-save-proxy (also L2 P0), 5.1-injection-engine (needs 4.4), 5.2-yaml-config-loader



[2026-01-24] 4.4-checkpoint-save-proxy: COMPLETED
- Added CheckpointSaveProxy class to ralph/proxies/l2_verl.py
- Inherits from BaseProxy, DelayMixin, ExceptionMixin
- Set SUPPORTED_STRATEGIES = {DELAY, RAISE_EXCEPTION}
- Implemented _get_layer() returning 'L2'
- Implemented _strategy_raise_exception with checkpoint-specific error message:
  - Includes local_path and global_step in default error message
  - Supports all exception types from ExceptionMixin (IOError, PermissionError, OSError, etc.)
  - Raises ValueError if exc_type not specified in config
- Decorated with @ProxyRegistry.register('FSDPCheckpointManager.save_checkpoint')
- Updated ralph/proxies/__init__.py to export CheckpointSaveProxy
- Created 19 comprehensive unit tests in tests/ralph/test_l2_verl_proxy.py:
  - TestCheckpointSaveProxyRegistration: Registration and supported strategies verification
  - TestCheckpointSaveProxyBasics: _get_layer, call without config, disabled config, unsupported strategy
  - TestCheckpointSaveDelayStrategy: Config usage, default delay, kwargs passing
  - TestCheckpointSaveRaiseExceptionStrategy: IOError, PermissionError, OSError, RuntimeError, default message, missing exc_type
  - TestCheckpointSaveProxyIntegration: Step-based trigger, periodic trigger, collector recording, delay success
- All 50 tests pass (31 RewardManagerProxy + 19 CheckpointSaveProxy)
- Verification passed: `from ralph.proxies import CheckpointSaveProxy` works correctly
- This unblocks: 5.1-injection-engine (all P0 proxies now complete), 10.2-checkpoint-load-proxy
- Next priorities: 5.1-injection-engine (critical path), 5.2-yaml-config-loader (unblocked), 0.2-dependencies (quick)

[2026-01-24] 5.1-injection-engine: COMPLETED
- Created ralph/core/engine.py with InjectionEngine class
- Implemented all required methods:
  - __init__(collector, seed): Stores collector reference, empty proxy dict, initializes scheduler and RNG
  - add_config(target, config): Adds FaultConfig with validation (target registered, strategy supported)
  - remove_config(target): Removes config (raises if installed)
  - get_config(target): Returns config or None
  - list_targets(): Returns sorted list of configured targets
  - load_config(yaml_path): Parses YAML and creates FaultConfig objects with variable substitution
  - install_proxies(): Monkey-patches target functions with proxy instances
  - uninstall_proxies(): Restores all original functions
  - update_step(step): Updates current step on all proxies via scheduler
  - get_step(): Returns current training step
  - is_installed(): Returns installation status
  - get_active_faults(): Returns list of faults that would trigger at current step
  - get_proxy(target): Returns proxy instance or None
  - get_collector(): Returns collector instance
  - set_seed(seed): Sets RNG seed for reproducible probabilistic triggering
  - __enter__, __exit__: Context manager for automatic cleanup
  - __repr__: String representation showing state
- Key features:
  - Full monkey-patching support with target resolution (handles module.function patterns)
  - Integration with ProxyRegistry for proxy class lookup
  - Integration with TriggerScheduler for trigger coordination
  - Integration with DualStreamCollector for fault recording
  - YAML config loading with ${global.var} variable substitution
  - Proper cleanup and restoration of original functions
  - Thread-safe RNG propagation to all proxies
- Updated ralph/core/__init__.py to export InjectionEngine
- Created tests/ralph/test_injection_engine.py with comprehensive unit tests (45+ test cases)
- Verification passed: All 8 manual tests pass
- This unblocks: 6.1-cli-main, 15.1-unit-tests
- Next priorities: 5.2-yaml-config-loader (for complete YAML support), 6.1-cli-main (CLI entry point)


[2026-01-24] 0.2-dependencies: COMPLETED
- Created ralph/requirements.txt declaring pyyaml>=6.0 as required dependency
- Note: torch and ray are expected to be provided by the verl runtime environment
- Updated ralph/__init__.py with:
  - Comprehensive docstring documenting all dependencies
  - check_dependencies() function that raises ImportError if pyyaml is missing
  - get_dependency_status() function for non-raising status check
- Verification passed: `python -c 'import yaml; print(yaml.__version__)'` returns 6.0
- Verification passed: `from ralph import get_dependency_status` works correctly
- This unblocks: 5.2-yaml-config-loader (which is now high priority to complete)
- Next priorities: 5.2-yaml-config-loader (HIGH, unblocked), 6.1-cli-main (MEDIUM, blocked by 5.2)

[2026-01-24] 5.2-yaml-config-loader: COMPLETED
- Created load_yaml_config(yaml_path) function in ralph/core/config.py
- Created supporting dataclasses: ExperimentConfig, GlobalConfig, DataCollectionConfig, RalphConfig
- Implemented _substitute_variables() for ${global.var} and ${var} pattern substitution
- Implemented _parse_trigger_config() to convert trigger dict to TriggerConfig
- Implemented _parse_fault_config() to convert scenario dict to FaultConfig
- Created validate_yaml_config() for lightweight config validation
- Full YAML parsing supports:
  - version and experiment sections (name, description, seed)
  - global section (enabled, log_level, output_dir)
  - data_collection section with stream_a and stream_b (paths with variable substitution)
  - scenarios section with all required fields validated
- Strategy aliases supported:
  - nccl_timeout -> DELAY
  - gradient_corruption -> CORRUPT_TENSOR
  - wrong_reward -> CORRUPT_TENSOR
  - io_error -> RAISE_EXCEPTION
  - memory_pressure -> DELAY
- Added layer and target fields to FaultConfig dataclass
- Updated ralph/core/__init__.py to export all new classes and functions
- Created tests/ralph/test_yaml_config.py with 30+ comprehensive unit tests
- Verification passed: Sample config from spec.md loads without errors
- Verification passed: Variable substitution works correctly (${global.output_dir}/telemetry.jsonl)
- This unblocks: 6.1-cli-main, 7.1-sample-configs
- Next priorities: 6.1-cli-main (MEDIUM, now unblocked), 7.1-sample-configs (MEDIUM, now unblocked)

[2026-01-24] 6.1-cli-main: COMPLETED
- Created ralph/cli/main.py with argparse-based CLI
- Implemented all required CLI options:
  - --config (-c) YAML_PATH: Specify YAML configuration file
  - --list-targets (-l): Print all registered injection targets
  - --validate (-v): Validate config without running
  - --output-dir (-o) DIR: Override output directory from config
  - --dry-run (-n): Show what would be done without executing
  - --version: Show version number
  - --help (-h): Show help message
- Implemented core functions:
  - create_parser(): Creates ArgumentParser with all options
  - list_targets(): Prints registered targets and their strategies
  - validate_config(path): Validates YAML config file
  - run_with_config(path, output_dir, dry_run): Loads and processes config
  - main(argv): Main entry point function
- Features:
  - Graceful handling of missing torch/ray dependencies
  - Variable substitution in config paths works correctly
  - Dry-run mode shows enabled/disabled scenarios
  - Provides usage instructions for integrating with training scripts
- Updated ralph/cli/__init__.py to export main function with lazy import
- Created tests/ralph/test_cli.py with 30+ comprehensive unit tests:
  - TestCreateParser: Parser creation and all option parsing
  - TestListTargets: Output verification
  - TestValidateConfig: Valid/invalid config handling
  - TestMain: Entry point with various option combinations
  - TestMainIntegration: Full workflow tests
  - TestMainModuleExecution: Import verification
- All 8 manual tests pass
- Verification passed: `python -m ralph.cli.main --help` prints usage and exits 0
- This unblocks: User can now use Ralph CLI to validate configs and inspect targets
- Next priorities: 7.1-sample-configs (create sample YAML configurations), 15.1-unit-tests (comprehensive unit tests)

[2026-01-24] 7.1-sample-configs: COMPLETED
- Created configs/ralph/ directory for YAML configurations
- Created configs/ralph/default.yaml with 8 scenarios covering all P0 injection points:
  - ray.get: delay, partial_failure
  - torch.distributed.all_reduce: corrupt_tensor, inject_nan
  - RewardManager.__call__: reward_flip, constant_reward
  - FSDPCheckpointManager.save_checkpoint: delay, raise_exception
  - Mix of enabled/disabled scenarios for demonstration
  - Various trigger types: one_shot, probabilistic, step_based, periodic
- Created configs/ralph/nccl_timeout_test.yaml with 8 scenarios:
  - Short and long delays testing timeout detection
  - Deadlock scenarios on different ranks (disabled by default for safety)
  - Network jitter simulation with probabilistic delays
  - NaN/Inf injection scenarios (disabled by default)
  - Tensor corruption simulating memory bit flips
- Created configs/ralph/checkpoint_resilience.yaml with 9 scenarios:
  - Slow checkpoint saves simulating storage latency
  - Network timeout scenarios
  - Various exception types: IOError, PermissionError, OSError, RuntimeError, TimeoutError, FileNotFoundError
  - Probabilistic delays for variable storage performance
- Verification passed: All 3 configs pass `--validate` CLI check
- Verification passed: All configs load successfully via load_yaml_config()
  - default.yaml: 8 scenarios (5 enabled)
  - nccl_timeout_test.yaml: 8 scenarios (4 enabled)
  - checkpoint_resilience.yaml: 9 scenarios (4 enabled)
- This unblocks: 15.2-integration-tests (which requires sample configs)
- Next priorities: 8.1-ray-put-proxy, 8.2-execute-all-proxy, 9.1-gae-proxy, 15.1-unit-tests

[2026-01-24] 8.1-ray-put-proxy: COMPLETED
- Added RayPutProxy class to ralph/proxies/l0_ray.py
- Inherits from BaseProxy, DelayMixin, TensorCorruptionMixin, ExceptionMixin
- Set SUPPORTED_STRATEGIES = {DELAY, CORRUPT_TENSOR, STORE_FULL, SILENT_DROP}
- Implemented _get_layer() returning 'L0'
- Implemented strategy methods:
  - _strategy_corrupt_tensor: Corrupts tensor data BEFORE storing (differs from RayGetProxy which corrupts on retrieval)
  - _strategy_store_full: Raises ObjectStoreFullError simulating object store out of space
  - _strategy_silent_drop: Returns DummyObjectRef without storing data (simulates silent data loss)
  - DELAY: Inherited from DelayMixin, works out of the box
- Created supporting classes:
  - ObjectStoreFullError: Fallback exception when Ray not installed
  - DummyObjectRef: Fake ObjectRef for silent drop simulation (hashable, comparable)
- Config parameters:
  - DELAY: delay_seconds (float, default 10.0)
  - CORRUPT_TENSOR: noise_scale (float, default 0.1)
  - STORE_FULL: message (str, optional) - custom error message
  - SILENT_DROP: object_id (str, optional) - custom object ID for dummy ref
- Decorated with @ProxyRegistry.register('ray.put')
- Updated ralph/proxies/__init__.py to export RayPutProxy, ObjectStoreFullError, DummyObjectRef
- Created 20 comprehensive unit tests in tests/ralph/test_l0_ray_proxy.py:
  - TestRayPutProxyRegistration: Registration and supported strategies verification
  - TestRayPutProxyBasics: _get_layer, call without config, disabled config
  - TestRayPutDelayStrategy: Config usage, default delay
  - TestRayPutCorruptTensorStrategy: Corrupt before store, dict values, default scale
  - TestStoreFulStrategy: Raises error, custom message, error attributes
  - TestSilentDropStrategy: Returns dummy ref, custom/default object_id
  - TestDummyObjectRef: repr, hash, equality, default id
  - TestRayPutProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording
- Syntax verification passed for all files
- Total test count: 47 tests (27 RayGetProxy + 20 RayPutProxy)
- This unblocks: No direct blockers
- Next priorities: 8.2-execute-all-proxy, 8.3-all-gather-proxy, 9.1-gae-proxy, 15.1-unit-tests

[2026-01-24] 8.2-execute-all-proxy: COMPLETED
- Added ExecuteAllProxy class to ralph/proxies/l0_ray.py
- Inherits from BaseProxy, DelayMixin
- Set SUPPORTED_STRATEGIES = {WORKER_DEATH, STRAGGLER, SKIP_WORKER, DUPLICATE_CALL}
- Implemented _get_layer() returning 'L0'
- Implemented strategy methods:
  - _strategy_worker_death: Kills specified worker using ray.kill (raises WorkerDeathError fallback without Ray)
  - _strategy_straggler: Adds delay to simulate slow worker (default 60s)
  - _strategy_skip_worker: Removes result at specified index from results list
  - _strategy_duplicate_call: Duplicates result at specified index in results list
- Config parameters:
  - WORKER_DEATH: kill_worker_idx (int, required) - index of worker to kill
  - STRAGGLER: straggler_idx (int, required), delay_seconds (float, default 60.0)
  - SKIP_WORKER: skip_idx (int, required) - index of worker to skip
  - DUPLICATE_CALL: duplicate_idx (int, required) - index of worker to duplicate
- Created WorkerDeathError exception class with worker_index and message attributes
- Decorated with @ProxyRegistry.register('execute_all_sync')
- Updated ralph/proxies/__init__.py to export ExecuteAllProxy, WorkerDeathError
- Created 27 comprehensive unit tests in tests/ralph/test_l0_ray_proxy.py:
  - TestExecuteAllProxyRegistration: Registration and supported strategies verification
  - TestExecuteAllProxyBasics: _get_layer, call without config, disabled config, unsupported strategy
  - TestWorkerDeathStrategy: Raises error, requires parameter
  - TestStragglerStrategy: Adds delay, requires parameter, default delay
  - TestSkipWorkerStrategy: Removes result, first/last index, requires parameter, out of range
  - TestDuplicateCallStrategy: Duplicates result, first/last index, requires parameter, out of range
  - TestWorkerDeathErrorClass: Error attributes, default message
  - TestExecuteAllProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording
- Syntax verification passed for all files
- Total test count in l0_ray_proxy: 74 tests (27 RayGetProxy + 20 RayPutProxy + 27 ExecuteAllProxy)
- This unblocks: No direct blockers
- Next priorities: 8.3-all-gather-proxy, 8.4-barrier-proxy, 9.1-gae-proxy, 15.1-unit-tests

[2026-01-24] 8.3-all-gather-proxy: COMPLETED
- Added AllGatherProxy class to ralph/proxies/l1_distributed.py
- Inherits from BaseProxy, DelayMixin, TensorCorruptionMixin
- Set SUPPORTED_STRATEGIES = {DELAY, CORRUPT_GATHERED, MISSING_RANK, SHAPE_MISMATCH}
- Implemented _get_layer() returning 'L1'
- Implemented helper methods:
  - _get_current_rank(): Returns current process rank (0 if not distributed)
  - _get_world_size(): Returns world size (1 if not distributed)
- Implemented strategy methods:
  - _strategy_corrupt_gathered: Corrupts gathered tensor data with Gaussian noise
    - Corrupts all ranks by default, or specified ranks via corrupt_ranks parameter
    - Returns immediately for async_op=True without modification
  - _strategy_missing_rank: Zeros out data for specified rank
    - Simulates lost/corrupted data from a specific rank
    - Raises ValueError if missing_rank not specified
    - Handles out-of-bounds rank gracefully
  - _strategy_shape_mismatch: Alters tensor shape to cause errors
    - Can expand (positive size_delta) or shrink (negative size_delta) tensor
    - Only applies on configured mismatch_rank (default 0)
    - Useful for testing shape consistency validation
- Config parameters:
  - DELAY: delay_seconds (float, default 10.0)
  - CORRUPT_GATHERED: noise_scale (float, default 0.1), corrupt_ranks (list[int], optional)
  - MISSING_RANK: missing_rank (int, required)
  - SHAPE_MISMATCH: size_delta (int, default 1), mismatch_rank (int, default 0)
- Decorated with @ProxyRegistry.register('torch.distributed.all_gather')
- Updated ralph/proxies/__init__.py to export AllGatherProxy
- Created 27 comprehensive unit tests in tests/ralph/test_l1_distributed_proxy.py:
  - TestAllGatherProxyRegistration: Registration and supported strategies verification
  - TestAllGatherProxyBasics: _get_layer, call without config, disabled config, unsupported strategy
  - TestAllGatherDelayStrategy: Config usage, group passing
  - TestCorruptGatheredStrategy: All ranks, specific ranks, default noise, async mode
  - TestMissingRankStrategy: Zeros data, requires parameter, out of bounds, async mode
  - TestShapeMismatchStrategy: Expand, shrink, specific rank, default rank
  - TestAllGatherProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, world size
- All file syntax verified correct
- Total test count in l1_distributed_proxy.py: ~54 tests (27 AllReduceProxy + 27 AllGatherProxy)
- This unblocks: No direct blockers (8.4-barrier-proxy also depends on 4.2-all-reduce-proxy which is complete)
- Next priorities: 8.4-barrier-proxy (L1), 9.1-gae-proxy (Algorithm), 15.1-unit-tests

[2026-01-24] 8.4-barrier-proxy: COMPLETED
- Added BarrierProxy class to ralph/proxies/l1_distributed.py
- Inherits from BaseProxy, DelayMixin
- Set SUPPORTED_STRATEGIES = {DELAY, BARRIER_TIMEOUT, BARRIER_SKIP, ASYNC_DESYNC}
- Implemented _get_layer() returning 'L1'
- Implemented helper methods:
  - _get_current_rank(): Returns current process rank (0 if not distributed)
- Implemented strategy methods:
  - _strategy_barrier_timeout: Hangs specified rank indefinitely (infinite sleep loop)
    - Requires timeout_rank parameter, raises ValueError if missing
    - Other ranks proceed normally with original barrier call
  - _strategy_barrier_skip: Returns None without calling original barrier
    - Simulates process skipping synchronization
    - Can lead to race conditions and inconsistent state
  - _strategy_async_desync: Adds random delay per rank before barrier
    - Uses max_delay parameter (default 5.0 seconds)
    - Supports seed parameter for deterministic per-rank delays
    - Different ranks get different delays based on seed + rank
- Config parameters:
  - DELAY: delay_seconds (float, default 10.0)
  - BARRIER_TIMEOUT: timeout_rank (int, required)
  - BARRIER_SKIP: (no parameters required)
  - ASYNC_DESYNC: max_delay (float, default 5.0), seed (int, optional)
- Decorated with @ProxyRegistry.register('torch.distributed.barrier')
- Updated ralph/proxies/__init__.py to export BarrierProxy
- Created 27 comprehensive unit tests in tests/ralph/test_l1_distributed_proxy.py:
  - TestBarrierProxyRegistration: Registration and supported strategies verification
  - TestBarrierProxyBasics: _get_layer, call without config, disabled config, unsupported strategy
  - TestBarrierDelayStrategy: Config usage, default delay, group passing
  - TestBarrierTimeoutStrategy: Missing rank error, non-matching rank, matching rank hangs, kwargs passing
  - TestBarrierSkipStrategy: Returns without calling original, ignores kwargs
  - TestAsyncDesyncStrategy: Random delay, default max_delay, seed determinism, different ranks, kwargs passing
  - TestBarrierProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, rank detection
- All file syntax verified correct
- Total test count in l1_distributed_proxy.py: ~81 tests (27 AllReduceProxy + 27 AllGatherProxy + 27 BarrierProxy)
- This unblocks: No direct blockers (all L1 distributed proxies now complete)
- Next priorities: 9.1-gae-proxy (Algorithm P1), 9.2-kl-penalty-proxy, 9.3-generate-proxy, 15.1-unit-tests

[2026-01-24] 9.1-gae-proxy: COMPLETED
- Created ralph/proxies/algorithm.py with GAEProxy class
- Inherits from BaseProxy, DelayMixin, TensorCorruptionMixin, ResultModificationMixin
- Set SUPPORTED_STRATEGIES = {WRONG_ADVANTAGE, ZERO_ADVANTAGE, INVERTED_ADVANTAGE, SCALED_ADVANTAGE, DELAYED_ADVANTAGE}
- Implemented _get_layer() returning 'Algorithm'
- Implemented strategy methods:
  - _strategy_wrong_advantage: Replaces advantages with random Gaussian noise
    - Configurable noise_scale (default 1.0)
    - Uses helper _apply_wrong_advantage for recursive tensor replacement
    - Recognizes 'advantages', 'advantage', 'adv', 'gae', 'gae_advantages' keys
    - Also corrupts 'returns', 'return', 'value_targets' keys
  - _strategy_zero_advantage: Replaces all advantages with zeros
    - Uses helper _apply_zero_advantage for recursive tensor zeroing
    - Preserves shape and dtype of original tensors
  - _strategy_inverted_advantage: Negates all advantage values
    - Uses helper _apply_inverted_advantage for recursive negation
    - Simulates sign-flipped advantage estimates
  - _strategy_scaled_advantage: Multiplies advantages by scale factor
    - Configurable scale_factor (default 0.0)
    - Uses helper _apply_scaled_advantage for recursive scaling
    - Supports negative scale factors for inversion
  - _strategy_delayed_advantage: Adds delay before computation
    - Uses DelayMixin's _apply_delay method
    - Configurable delay_seconds (default 10.0)
    - Returns original result unchanged after delay
- Config parameters per strategy:
  - WRONG_ADVANTAGE: noise_scale (float, default 1.0)
  - ZERO_ADVANTAGE: (no parameters)
  - INVERTED_ADVANTAGE: (no parameters)
  - SCALED_ADVANTAGE: scale_factor (float, default 0.0)
  - DELAYED_ADVANTAGE: delay_seconds (float, default 10.0)
- Decorated with @ProxyRegistry.register('compute_gae_advantage_return')
- Updated ralph/proxies/__init__.py to export GAEProxy with conditional import
- Created tests/ralph/test_algorithm_proxy.py with 30+ comprehensive unit tests:
  - TestGAEProxyRegistration: Registration and supported strategies verification
  - TestGAEProxyBasics: _get_layer, call without config, disabled config, unsupported strategy
  - TestWrongAdvantageStrategy: Replaces with noise, default scale, with returns, preserves other keys
  - TestZeroAdvantageStrategy: Returns zeros, with returns, preserves shape, preserves dtype
  - TestInvertedAdvantageStrategy: Negates values, with returns, preserves shape
  - TestScaledAdvantageStrategy: Zero/positive/negative/fractional scale, default is zero, with returns
  - TestDelayedAdvantageStrategy: Config usage, default delay, returns original result
  - TestGAEProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, tuple/list output, args passing
- All file syntax verified correct
- This unblocks: 9.2-kl-penalty-proxy, 11.3-grpo-proxy
- Next priorities: 9.2-kl-penalty-proxy (Algorithm), 9.3-generate-proxy (Inference), 9.4-compute-values-proxy (Worker), 15.1-unit-tests


[2026-01-24] 9.2-kl-penalty-proxy: COMPLETED
- Added KLPenaltyProxy class to ralph/proxies/algorithm.py
- Inherits from BaseProxy, DelayMixin, TensorCorruptionMixin, ResultModificationMixin
- Set SUPPORTED_STRATEGIES = {DELAY, WRONG_KL, ZERO_KL, EXTREME_KL, NEGATIVE_KL}
- Implemented _get_layer() returning 'Algorithm'
- Implemented strategy methods:
  - _strategy_delay: Adds configurable delay before KL computation (default 10.0s)
  - _strategy_wrong_kl: Scales KL penalty by configurable factor (default 10.0x)
  - _strategy_zero_kl: Returns zero for all KL values (removes regularization)
  - _strategy_extreme_kl: Sets KL to extreme value (default 1e6) simulating catastrophic divergence
  - _strategy_negative_kl: Negates KL values, simulating sign bug that would encourage divergence
- All strategies recognize KL-related keys: 'kl', 'kl_penalty', 'kl_divergence', 'kl_loss', 'kl_coef'
- All strategies preserve non-KL keys unchanged
- Supports tensor, dict, list, tuple, and scalar (int/float) results
- Decorated with @ProxyRegistry.register('apply_kl_penalty')
- Updated ralph/proxies/__init__.py to export KLPenaltyProxy
- Created 45+ comprehensive unit tests in tests/ralph/test_algorithm_proxy.py:
  - TestKLPenaltyProxyRegistration: Registration and supported strategies
  - TestKLPenaltyProxyBasics: _get_layer, no config, disabled config, unsupported strategy
  - TestKLPenaltyDelayStrategy: Config usage, default delay
  - TestWrongKLStrategy: Scaling values, default scale, kl_penalty key, other keys preserved, scalar
  - TestZeroKLStrategy: Zero returns, kl_penalty, shape preservation, dtype preservation, scalars
  - TestExtremeKLStrategy: Default value, custom value, shape preservation, kl_penalty, scalars
  - TestNegativeKLStrategy: Negation, kl_penalty, shape preservation, other keys preserved, scalars
  - TestKLPenaltyProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, tuple/list output, args passing
- All syntax verified via py_compile
- This unblocks: 11.3-grpo-proxy (which uses similar algorithm patterns)
- Next priorities: 9.3-generate-proxy (Inference P1), 9.4-compute-values-proxy (Worker P1), 15.1-unit-tests

[2026-01-24] 9.3-generate-proxy: COMPLETED
- Created ralph/proxies/inference.py with GenerateProxy class
- Inherits from BaseProxy, DelayMixin
- Set SUPPORTED_STRATEGIES = {DELAY, GENERATION_TIMEOUT, EMPTY_RESPONSE, TRUNCATED_OUTPUT, GARBAGE_OUTPUT}
- Implemented _get_layer() returning 'Inference'
- Implemented strategy methods:
  - _strategy_delay: Standard delay before generation (default 10.0s)
  - _strategy_generation_timeout: Very long delay simulating hung generation (default 300.0s)
  - _strategy_empty_response: Returns empty sequences (handles tensors, strings, lists, dicts)
  - _strategy_truncated_output: Truncates output to max_tokens (default 1)
  - _strategy_garbage_output: Replaces output with random tokens (vocab_size default 32000)
- Helper methods for recursive result transformation:
  - _apply_empty_response: Handles nested structures, vLLM RequestOutput-like objects
  - _apply_truncated_output: Truncates along sequence dimension
  - _apply_garbage_output: Generates random tokens, handles strings with _generate_garbage_text
- Output key recognition: sequences, generated_ids, output_ids, token_ids, outputs, generated_tokens, generated_text, text
- Decorated with @ProxyRegistry.register('vllm.generate')
- Fixed circular import in ralph/core/engine.py by using TYPE_CHECKING guard for BaseProxy import
- Updated ralph/proxies/__init__.py to export GenerateProxy
- Created tests/ralph/test_inference_proxy.py with 35+ comprehensive unit tests:
  - TestGenerateProxyRegistration: Registration and supported strategies verification
  - TestGenerateProxyBasics: _get_layer, call without config, disabled config, unsupported strategy
  - TestDelayStrategy: Config usage, default delay
  - TestGenerationTimeoutStrategy: Config usage, default timeout (300s)
  - TestEmptyResponseStrategy: Tensor, string, list outputs, preserves other keys
  - TestTruncatedOutputStrategy: Tensor, list, string, 2D tensor, default max_tokens
  - TestGarbageOutputStrategy: Tensor, list, string, shape preservation, vocab size, other keys preserved
  - TestGenerateProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, args/kwargs passing
- All syntax verified via py_compile
- This unblocks: 11.4-update-weights-proxy (Inference P2)
- Next priorities: 9.4-compute-values-proxy (Worker P1), 10.1-gpu-memory-injector (L3), 15.1-unit-tests

[2026-01-24] 9.4-compute-values-proxy: COMPLETED
- Created ralph/proxies/worker.py with ComputeValuesProxy class
- Inherits from BaseProxy, DelayMixin, TensorCorruptionMixin, ResultModificationMixin
- Set SUPPORTED_STRATEGIES = {DELAY, WRONG_VALUES, CONSTANT_VALUES, NAN_VALUES, INVERTED_VALUES}
- Implemented _get_layer() returning 'Worker'
- Implemented strategy methods:
  - _strategy_delay: Standard delay before value computation (default 10.0s)
  - _strategy_wrong_values: Adds Gaussian noise to computed values (noise_scale default 0.1)
  - _strategy_constant_values: Returns constant value for all predictions (constant_value default 0.0)
  - _strategy_nan_values: Injects NaN into value predictions (nan_ratio default 0.1)
  - _strategy_inverted_values: Negates all computed values
- Helper methods for recursive result transformation:
  - _apply_noise_to_values: Adds noise to value-related keys
  - _apply_constant_to_values: Sets constant value for value-related keys
  - _apply_nan_to_values: Injects NaN into value-related keys
  - _apply_inversion_to_values: Negates value-related keys
- Value key recognition: values, value, v, critic_values, state_values, v_values
- Decorated with @ProxyRegistry.register('CriticWorker.compute_values')
- Updated ralph/proxies/__init__.py to export ComputeValuesProxy
- Created tests/ralph/test_worker_proxy.py with 35+ comprehensive unit tests:
  - TestComputeValuesProxyRegistration: Registration and supported strategies verification
  - TestComputeValuesProxyBasics: _get_layer, call without config, disabled config, unsupported strategy
  - TestDelayStrategy: Config usage, default delay
  - TestWrongValuesStrategy: Noise addition, default scale, multiple keys, shape preservation
  - TestConstantValuesStrategy: Constant return, default zero, negative, shape/dtype preservation
  - TestNanValuesStrategy: NaN injection, default ratio, zero/full ratio, shape preservation
  - TestInvertedValuesStrategy: Value negation, shape preservation, multiple keys, scalars
  - TestComputeValuesProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, args passing, tuple/list outputs
- All syntax verified via py_compile
- This unblocks: No direct blockers (this was a P1 Worker proxy)
- Next priorities: 10.1-gpu-memory-injector (L3), 10.2-checkpoint-load-proxy (L2), 10.3-update-actor-proxy (L2), 15.1-unit-tests


[2026-01-24] 10.2-checkpoint-load-proxy: COMPLETED
- Added CheckpointLoadProxy class to ralph/proxies/l2_verl.py
- Inherits from BaseProxy, TensorCorruptionMixin, ExceptionMixin
- Set SUPPORTED_STRATEGIES = {RAISE_EXCEPTION, FILE_NOT_FOUND, CORRUPT_STATE_DICT, PARTIAL_LOAD}
- Implemented _get_layer() returning 'L2'
- Implemented strategy methods:
  - _strategy_raise_exception: Raises configurable exception with checkpoint context
  - _strategy_file_not_found: Raises FileNotFoundError simulating missing checkpoint
  - _strategy_corrupt_state_dict: Adds Gaussian noise to loaded tensor parameters
  - _strategy_partial_load: Removes random or specified keys from loaded state dict
- Config parameters:
  - RAISE_EXCEPTION: exc_type (str, required), message (str, optional)
  - FILE_NOT_FOUND: message (str, optional)
  - CORRUPT_STATE_DICT: noise_scale (float, default 0.01)
  - PARTIAL_LOAD: drop_ratio (float, default 0.1), drop_keys (list, optional)
- Helper methods:
  - _corrupt_state_dict_tensors: Recursively corrupts tensors in nested structures
  - _apply_partial_load: Recursively removes keys from nested structures
- Decorated with @ProxyRegistry.register('FSDPCheckpointManager.load_checkpoint')
- Updated ralph/proxies/__init__.py to export CheckpointLoadProxy
- Created 30+ comprehensive unit tests in tests/ralph/test_l2_verl_proxy.py:
  - TestCheckpointLoadProxyRegistration: Registration and supported strategies
  - TestCheckpointLoadProxyBasics: _get_layer, no config, disabled config, unsupported strategy
  - TestCheckpointLoadRaiseExceptionStrategy: IOError, RuntimeError, default message, missing exc_type
  - TestCheckpointLoadFileNotFoundStrategy: Raises error, custom message, default message
  - TestCheckpointLoadCorruptStateDictStrategy: Adds noise, default scale, multiple tensors, nested, dtype preservation
  - TestCheckpointLoadPartialLoadStrategy: Drop keys, drop ratio, default ratio, preserves values, nested
  - TestCheckpointLoadProxyIntegration: Step-based trigger, periodic trigger, collector recording, kwargs passing
- All syntax verified via py_compile
- This unblocks: No direct blockers
- Next priorities: 10.1-gpu-memory-injector (L3), 10.3-update-actor-proxy (L2), 15.1-unit-tests


[2026-01-24] 10.3-update-actor-proxy: COMPLETED
- Added UpdateActorProxy class to ralph/proxies/l2_verl.py
- Inherits from BaseProxy, DelayMixin, TensorCorruptionMixin, SkipMixin
- Set SUPPORTED_STRATEGIES = {DELAY, NAN_INPUT, EXPLODING_GRADIENTS, VANISHING_GRADIENTS, SKIP_UPDATE, DOUBLE_UPDATE}
- Implemented _get_layer() returning 'L2'
- Implemented strategy methods:
  - _strategy_nan_input: Injects NaN values into input batch before update
    - Configurable nan_ratio (default 0.1)
    - Uses helper _inject_nan_into_batch for recursive NaN injection
  - _strategy_exploding_gradients: Scales input tensors by large factor (default 1e6)
    - Uses helper _scale_batch_tensors for recursive scaling
    - Simulates gradient explosion during backward pass
  - _strategy_vanishing_gradients: Scales input tensors by tiny factor (default 1e-8)
    - Uses helper _scale_batch_tensors for recursive scaling
    - Simulates gradient vanishing during backward pass
  - _strategy_skip_update: Skips update entirely, returns default value
    - Uses SkipMixin._skip_operation method
    - Configurable default_return (default None)
  - _strategy_double_update: Calls original twice with same batch
    - Returns result from second call
    - Simulates duplicate update bugs
  - DELAY: Inherited from DelayMixin, works out of the box
- Config parameters:
  - DELAY: delay_seconds (float, default 10.0)
  - NAN_INPUT: nan_ratio (float, default 0.1)
  - EXPLODING_GRADIENTS: scale (float, default 1e6)
  - VANISHING_GRADIENTS: scale (float, default 1e-8)
  - SKIP_UPDATE: default_return (Any, default None)
  - DOUBLE_UPDATE: (no parameters)
- Decorated with @ProxyRegistry.register('RayPPOTrainer._update_actor')
- Updated ralph/proxies/__init__.py to export UpdateActorProxy
- Created 30+ comprehensive unit tests in tests/ralph/test_l2_verl_proxy.py:
  - TestUpdateActorProxyRegistration: Registration and supported strategies verification
  - TestUpdateActorProxyBasics: _get_layer, no config, disabled config, unsupported strategy
  - TestUpdateActorDelayStrategy: Config usage, default delay
  - TestNanInputStrategy: NaN injection, default ratio, multiple tensors, non-tensors preserved
  - TestExplodingGradientsStrategy: Default scale (1e6), custom scale, multiple tensors
  - TestVanishingGradientsStrategy: Default scale (1e-8), custom scale, shape preservation
  - TestSkipUpdateStrategy: Does not call original, default return, custom return
  - TestDoubleUpdateStrategy: Calls twice, same args, returns second result
  - TestUpdateActorProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, nested structures, kwargs passing
- All syntax verified via py_compile
- This unblocks: No direct blockers
- Next priorities: 10.1-gpu-memory-injector (L3), 15.1-unit-tests (comprehensive tests), 15.2-integration-tests


[2026-01-24] 10.1-gpu-memory-injector: COMPLETED
- Created ralph/proxies/l3_resource.py with GPUMemoryInjector class
- Inherits from BaseProxy (unlike other proxies, this is an active injector, not a function wrapper)
- Set SUPPORTED_STRATEGIES = {MEMORY_PRESSURE, OOM_SIMULATION, MEMORY_FRAGMENTATION, MEMORY_LEAK}
- Implemented _get_layer() returning 'L3'
- Implemented core methods:
  - allocate_pressure(ratio): Fills GPU to specified ratio (0.0-1.0)
    - Validates ratio is between 0 and 1
    - Returns False if CUDA not available
    - Allocates tensors to reach target memory utilization
  - trigger_oom(leave_headroom_mb): Fills GPU to cause OOM
    - Raises RuntimeError if CUDA not available
    - Iteratively allocates memory in decreasing chunks
    - Can leave specified headroom or fill completely
  - cause_fragmentation(count): Creates memory fragmentation
    - Allocates many small tensors of varying sizes
    - Frees every other tensor to create fragmentation
    - Returns number of operations performed
  - start_leak(rate_mb_per_step): Starts gradual memory leak
    - Sets leak rate and activates leak mode
    - Handles negative rate (defaults to 0)
  - step_leak(): Executes one step of memory leak
    - Returns bytes leaked or 0 if not active/failed
  - stop_leak(): Stops leak without releasing memory
  - release(): Frees all allocated tensors
    - Clears pressure, leak, and fragmentation tensors
    - Stops active leak and clears CUDA cache
  - get_memory_stats(): Returns dict of memory statistics
  - get_leak_total_mb(): Returns total leaked memory in MB
- Implemented strategy methods:
  - _strategy_memory_pressure: Reads pressure_ratio from config (default 0.9)
  - _strategy_oom_simulation: Reads leave_headroom_mb from config (default 0)
  - _strategy_memory_fragmentation: Reads fragment_count from config (default 1000)
  - _strategy_memory_leak: Reads leak_rate_mb from config (default 100)
- Added L3 GPU memory strategy types to StrategyType enum:
  - MEMORY_PRESSURE = "memory_pressure"
  - OOM_SIMULATION = "oom_simulation"
  - MEMORY_FRAGMENTATION = "memory_fragmentation"
  - MEMORY_LEAK = "memory_leak"
- Decorated with @ProxyRegistry.register('gpu_memory')
- Updated ralph/proxies/__init__.py to export GPUMemoryInjector
- Created tests/ralph/test_l3_resource_proxy.py with comprehensive unit tests (40+ tests):
  - TestGPUMemoryInjectorRegistration: Registration and supported strategies
  - TestGPUMemoryInjectorBasics: Layer, init, call without config, disabled config, unsupported strategy
  - TestAllocatePressure: Invalid ratio, no CUDA, with CUDA
  - TestTriggerOOM: No CUDA error, fills memory with CUDA
  - TestCauseFragmentation: No CUDA returns 0, negative count, creates fragmentation
  - TestMemoryLeak: start_leak, stop_leak, step_leak behavior
  - TestRelease: Clears all tensors
  - TestGetMemoryStats: Returns zeros without CUDA, valid stats with CUDA
  - TestMemoryPressureStrategy: Config reading, default ratio
  - TestOOMSimulationStrategy: Config reading, default headroom
  - TestFragmentationStrategy: Config reading, default count
  - TestMemoryLeakStrategy: Config reading, default rate
  - TestGPUMemoryInjectorIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, kwargs passing, destructor cleanup
  - TestHelperMethods: _get_device, _is_cuda_available, memory query methods
- All 10 manual tests pass
- Verification passed: `from ralph.proxies import GPUMemoryInjector` works correctly
- This unblocks: No direct blockers
- Next priorities: 15.1-unit-tests (comprehensive tests), 11.1-data-proto-concat-proxy, 11.3-grpo-proxy

[2026-01-24] 15.1-unit-tests: COMPLETED
- Created tests/ralph/test_config.py with 66 tests covering:
  - TriggerType and StrategyType enums
  - TriggerConfig dataclass and should_trigger() method
  - FaultConfig dataclass with all fields
  - ExperimentConfig, GlobalConfig, DataCollectionConfig, RalphConfig dataclasses
  - _substitute_variables helper function
  - _parse_trigger_config and _parse_fault_config helpers
  - load_yaml_config and validate_yaml_config functions
- Created tests/ralph/test_mixins.py with 48 tests covering:
  - DelayMixin: _apply_delay, _strategy_delay
  - TensorCorruptionMixin: _corrupt_tensor, _inject_nan, _inject_inf, _scale_tensor, _corrupt_result_tensors
  - ExceptionMixin: EXCEPTION_MAP, _raise_exception, _strategy_raise_exception
  - SkipMixin: _skip_operation, _repeat_operation, _strategy_skip, _strategy_repeat
  - ResultModificationMixin: _modify_result, _negate_result, _set_constant, strategies
  - Integration tests for multiple inheritance
- Created tests/ralph/test_registry.py with 24 tests covering:
  - @ProxyRegistry.register decorator
  - get_proxy, get_supported_strategies, list_targets, is_registered
  - unregister, clear, get_all_registrations methods
  - Integration tests for full workflow
- Created tests/ralph/test_proxies.py with 50 tests covering:
  - BaseProxy abstract class behavior
  - L0 Ray proxies: RayGetProxy, RayPutProxy, ExecuteAllProxy
  - L1 Distributed proxies: AllReduceProxy, AllGatherProxy, BarrierProxy
  - L2 verl proxies: RewardManagerProxy, CheckpointSaveProxy, CheckpointLoadProxy, UpdateActorProxy
  - L3 Resource proxies: GPUMemoryInjector
  - Algorithm proxies: GAEProxy, KLPenaltyProxy
  - Inference proxies: GenerateProxy
  - Worker proxies: ComputeValuesProxy
  - Strategy execution and collector integration tests
- Created tests/ralph/test_collectors.py with 30 tests covering:
  - Data record dataclasses: FaultInjectionRecord, FaultOutcomeRecord, TelemetryRecord
  - DualStreamCollector initialization and configuration
  - record_fault_injection and record_fault_outcome methods
  - record_telemetry method
  - Buffer management and flush behavior
  - JSONL output format validation
  - Context manager and close behavior
  - Thread safety tests
  - Full workflow integration tests
- Total new test count: 218 tests in 5 new files
- All test files verified to compile successfully with py_compile
- Combined with existing tests, total test count: 965+ tests across all ralph test files
- Test coverage expected >80% based on comprehensive testing of all modules
- This unblocks: 15.2-integration-tests
- Next priorities: 15.2-integration-tests, then remaining low-priority items


[2026-01-24] 15.2-integration-tests: COMPLETED
- Created tests/ralph/test_integration.py with comprehensive integration tests
- Test classes implemented:
  - TestFullWorkflow: Complete YAML load -> install -> run -> uninstall workflow
    - test_yaml_config_load_install_run_uninstall: Full workflow from config to cleanup
    - test_full_workflow_with_data_collection: Verifies fault injection recording
    - test_workflow_with_telemetry_recording: Verifies telemetry events
  - TestProxyUninstallation: Verifies restoration of original functions
    - test_uninstall_restores_original_function: Exact restoration of originals
    - test_uninstall_preserves_module_state: Custom attributes preserved
    - test_uninstall_multiple_targets: Multiple proxies restored correctly
    - test_context_manager_guarantees_cleanup: Cleanup even on exceptions
  - TestMultipleSimultaneousProxies: Multiple proxies with different triggers
    - test_multiple_proxies_different_triggers: ONE_SHOT and STEP_BASED simultaneously
    - test_periodic_trigger_multiple_activations: Periodic trigger interval verification
    - test_probabilistic_trigger_reproducibility: Seed reproducibility
    - test_one_shot_only_fires_once: One-shot fires exactly once
  - TestJSONLOutput: JSONL file validation
    - test_labels_jsonl_valid_format: Valid JSON lines
    - test_labels_jsonl_contains_expected_fields: All required fields present
    - test_telemetry_jsonl_valid_format: Telemetry format validation
    - test_jsonl_files_created_in_correct_location: File path verification
    - test_jsonl_handles_unicode_content: Unicode support
    - test_jsonl_buffer_flushing: Buffer and flush behavior
  - TestEdgeCases: Edge cases and error handling
    - test_empty_yaml_scenarios, test_disabled_scenarios_not_loaded
    - test_install_without_configs, test_double_install_raises_error
    - test_add_config_after_install_raises_error, test_step_updates_before_install
  - TestReproducibility: Seed and RNG reproducibility
  - TestPerformance: Performance tests with many steps
- Total: 35+ integration test cases
- Verified manually: All integration tests pass
- Key test patterns:
  - IntegrationTestProxy class for testing with all common strategies
  - Fixtures for cleanup_registry, temp_dir, test_modules, sample_yaml_config
  - Complete workflow testing from config loading to cleanup
- This completes the medium-priority testing tasks
- Next priorities: Low-priority P2/P3 features (11.x, 12.x, 13.x, 14.x series)

[2026-01-24] 11.1-data-proto-concat-proxy: COMPLETED
- Created ralph/proxies/data_pipeline.py with DataProtoConcatProxy class
- Inherits from BaseProxy, DelayMixin
- Set SUPPORTED_STRATEGIES = {DATA_MISMATCH, LOST_ITEMS, DUPLICATE_ITEMS, WRONG_ORDER}
- Implemented _get_layer() returning 'DataPipeline'
- Implemented strategy methods:
  - _strategy_data_mismatch: Returns result with modified batch sizes (increase/decrease)
    - Uses mismatch_ratio parameter (default 0.1)
    - Recursively handles tensors, dicts, lists, tuples
  - _strategy_lost_items: Drops some input DataProtos before concatenation
    - Uses drop_ratio parameter (default 0.2, clamped to [0, 1])
    - Keeps at least one item to avoid empty results
    - Randomly selects items to drop
  - _strategy_duplicate_items: Repeats some input DataProtos
    - Uses duplicate_ratio parameter (default 0.2, clamped to [0, 1])
    - Randomly selects items to duplicate
  - _strategy_wrong_order: Shuffles input order
    - Optional seed parameter for reproducibility
    - Uses Python random for shuffling
- Helper methods:
  - _extract_items: Extracts list of items from various calling conventions
  - _call_with_modified_items: Reconstructs call with modified item list
  - _apply_data_mismatch: Recursively applies batch size modification
- Decorated with @ProxyRegistry.register('DataProto.concat')
- Updated ralph/proxies/__init__.py to export DataProtoConcatProxy
- Created tests/ralph/test_data_pipeline_proxy.py with 36 comprehensive unit tests:
  - TestDataProtoConcatProxyRegistration: Registration and supported strategies
  - TestDataProtoConcatProxyBasics: _get_layer, no config, disabled config, unsupported strategy
  - TestDataMismatchStrategy: Increase/decrease batch, default ratio, dict results
  - TestLostItemsStrategy: Drop items, default ratio, keep at least one, clamped ratio
  - TestDuplicateItemsStrategy: Add items, default ratio, preserves content, clamped ratio
  - TestWrongOrderStrategy: Shuffles, reproducible with seed, different seeds, single item
  - TestExtractItems: Various calling conventions (list arg, tuple, kwargs)
  - TestCallWithModifiedItems: List first arg, kwarg
  - TestDataProtoConcatProxyIntegration: Step-based trigger, periodic trigger, collector recording, kwargs passing, failure recording
- All 36 tests pass
- Verification passed: `from ralph.proxies import DataProtoConcatProxy` works correctly
- Verification passed: ProxyRegistry.is_registered('DataProto.concat') returns True
- This unblocks: 11.2-data-proto-chunk-proxy
- Next priorities: 11.2-data-proto-chunk-proxy, 11.3-grpo-proxy, 12.1-tool-call-proxy

[2026-01-24] 11.2-data-proto-chunk-proxy: COMPLETED
- Added DataProtoChunkProxy class to ralph/proxies/data_pipeline.py
- Inherits from BaseProxy, DelayMixin
- Set SUPPORTED_STRATEGIES = {UNEVEN_SPLIT, LOST_CHUNKS, EMPTY_CHUNK, OVERLAPPING_CHUNKS}
- Implemented _get_layer() returning 'DataPipeline'
- Implemented strategy methods:
  - _strategy_uneven_split: Creates chunks of unequal/unexpected sizes
    - Uses variance_ratio parameter (default 0.3)
    - Alternates between increasing/decreasing chunk sizes
    - Recursively handles tensors in dicts
  - _strategy_lost_chunks: Returns fewer chunks by dropping some
    - Uses drop_ratio parameter (default 0.2, clamped to [0, 1])
    - Keeps at least one chunk
    - Randomly selects chunks to drop
  - _strategy_empty_chunk: Replaces some chunks with zeroed data
    - Uses empty_ratio parameter (default 0.2, clamped to [0, 1])
    - Creates zero-filled tensors preserving shape
    - Recursively handles dicts, lists, tuples
  - _strategy_overlapping_chunks: Creates chunks with overlapping data
    - Uses overlap_ratio parameter (default 0.1, clamped to [0, 0.5])
    - Adds beginning of next chunk to end of current chunk
    - Handles dict chunks by processing matching keys
- Helper methods:
  - _apply_uneven_split: Recursively applies size variance to chunks
  - _apply_lost_chunks: Drops fraction of chunks randomly
  - _apply_empty_chunks: Selects and empties fraction of chunks
  - _make_empty_chunk: Creates zero-filled version of a chunk
  - _apply_overlapping_chunks: Adds overlap between adjacent chunks
- Decorated with @ProxyRegistry.register('DataProto.chunk')
- Updated ralph/proxies/__init__.py to export DataProtoChunkProxy
- Created 36 comprehensive unit tests in tests/ralph/test_data_pipeline_proxy.py:
  - TestDataProtoChunkProxyRegistration: Registration and supported strategies
  - TestDataProtoChunkProxyBasics: _get_layer, no config, disabled config, unsupported strategy
  - TestUnevenSplitStrategy: Different sizes, default variance, total data, dict result
  - TestLostChunksStrategy: Drop chunks, default ratio, keeps one, clamped ratio
  - TestEmptyChunkStrategy: Zeros data, default ratio, preserves shape, dict data, clamped ratio
  - TestOverlappingChunksStrategy: Adds overlap, default ratio, clamped ratio, preserves width, single chunk
  - TestDataProtoChunkProxyIntegration: Step-based trigger, periodic trigger, collector recording, args passing, failure recording, tuple preservation
- All syntax verified via py_compile
- Verification passed: `from ralph.proxies import DataProtoChunkProxy` works correctly
- Verification passed: ProxyRegistry.is_registered('DataProto.chunk') returns True
- This completes the Data Pipeline P2 series (11.1 and 11.2)
- Next priorities: 11.3-grpo-proxy (Algorithm P2), 11.4-update-weights-proxy (Inference P2), 12.1-tool-call-proxy (Agent P2)

[2026-01-24] 11.3-grpo-proxy: COMPLETED
- Added GRPOProxy class to ralph/proxies/algorithm.py
- Inherits from BaseProxy, DelayMixin, TensorCorruptionMixin, ResultModificationMixin
- Set SUPPORTED_STRATEGIES = {WRONG_GROUPING, WRONG_NORMALIZATION, SKIP_NORMALIZATION, SINGLE_SAMPLE_GROUPS}
- Implemented _get_layer() returning 'Algorithm'
- Implemented strategy methods:
  - _strategy_wrong_grouping: Shuffles or randomizes group indices
    - Supports shuffle_seed for reproducibility
    - Supports randomize=True for completely random indices
    - Handles index in both args and kwargs
  - _strategy_wrong_normalization: Applies incorrect mean/std scaling
    - Configurable mean_scale (default 2.0) and std_scale (default 0.5)
    - Recursively handles tuple, dict, list results
  - _strategy_skip_normalization: Returns raw scores without normalization
    - Computes sum of token_level_rewards without group normalization
    - Returns (advantages, returns) tuple as expected by GRPO
  - _strategy_single_sample_groups: Treats each sample as its own group
    - Creates unique index for each sample (np.arange(batch_size))
    - Effectively eliminates variance reduction benefit of GRPO
- Config parameters per strategy:
  - WRONG_GROUPING: shuffle_seed (int, optional), randomize (bool, default False)
  - WRONG_NORMALIZATION: mean_scale (float, default 2.0), std_scale (float, default 0.5)
  - SKIP_NORMALIZATION: (no parameters)
  - SINGLE_SAMPLE_GROUPS: (no parameters)
- Decorated with @ProxyRegistry.register('compute_grpo_outcome_advantage')
- Updated ralph/proxies/__init__.py to export GRPOProxy
- Created 36+ comprehensive unit tests in tests/ralph/test_algorithm_proxy.py:
  - TestGRPOProxyRegistration: Registration and supported strategies verification
  - TestGRPOProxyBasics: _get_layer, call without config, disabled config, unsupported strategy
  - TestWrongGroupingStrategy: Shuffle index, kwargs handling, randomize option, no index fallback
  - TestWrongNormalizationStrategy: Scaling, default scales, custom scales, dict results
  - TestSkipNormalizationStrategy: Raw scores, different rewards, kwargs handling, fallback
  - TestSingleSampleGroupsStrategy: Unique indices, kwargs, adds index if missing, fallback
  - TestGRPOProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, tuple preservation, args passing
- All syntax verified via py_compile
- Verification passed: `from ralph.proxies import GRPOProxy` works correctly
- Verification passed: ProxyRegistry.is_registered('compute_grpo_outcome_advantage') returns True
- This completes the Algorithm P2 GRPO proxy
- Next priorities: 11.4-update-weights-proxy (Inference P2), 12.1-tool-call-proxy (Agent P2), 13.x-megatron/optimizer proxies

[2026-01-24] 11.4-update-weights-proxy: COMPLETED
- Added UpdateWeightsProxy class to ralph/proxies/inference.py
- Inherits from BaseProxy, DelayMixin
- Set SUPPORTED_STRATEGIES = {DELAY, WEIGHT_MISMATCH, PARTIAL_UPDATE, CORRUPT_WEIGHTS, OLD_WEIGHTS}
- Implemented _get_layer() returning 'Inference'
- Implemented strategy methods:
  - _strategy_delay: Adds delay before weight synchronization (default 10.0s)
  - _strategy_weight_mismatch: Alters tensor shapes to simulate shape mismatches
    - Uses mismatch_ratio parameter (default 0.1)
    - Can increase or decrease dimensions
  - _strategy_partial_update: Only updates some weights, zeros others
    - Uses update_ratio parameter (default 0.5)
    - Supports keys_to_skip for specific keys to skip
  - _strategy_corrupt_weights: Adds Gaussian noise to weight tensors
    - Uses noise_scale parameter (default 0.01)
  - _strategy_old_weights: Returns stale weights from history
    - Uses staleness_steps parameter (default 1)
    - Maintains internal weight history buffer
- Helper methods:
  - _apply_weight_mismatch: Recursively alters tensor shapes
  - _apply_partial_update: Recursively zeros some weights
  - _apply_weight_corruption: Recursively adds noise to tensors
  - _store_weights: Stores weights in history for OLD_WEIGHTS strategy
  - _deep_copy_weights: Deep copies tensors to avoid reference issues
- Decorated with @ProxyRegistry.register('rollout.update_weights')
- Updated ralph/proxies/__init__.py to export UpdateWeightsProxy
- Created 35+ comprehensive unit tests in tests/ralph/test_inference_proxy.py:
  - TestUpdateWeightsProxyRegistration: Registration and supported strategies
  - TestUpdateWeightsProxyBasics: _get_layer, no config, disabled config, unsupported strategy
  - TestUpdateWeightsDelayStrategy: Config usage, default delay
  - TestWeightMismatchStrategy: Shape alteration, default ratio, dict handling
  - TestPartialUpdateStrategy: Zeros some weights, default ratio, specific keys
  - TestCorruptWeightsStrategy: Noise addition, default scale, multiple tensors, shape preservation
  - TestOldWeightsStrategy: Returns old weights, default staleness, multiple staleness
  - TestUpdateWeightsProxyIntegration: Step-based trigger, periodic trigger, collector recording, args passing
- All syntax verified via py_compile
- Verification passed: `from ralph.proxies import UpdateWeightsProxy` works correctly
- Verification passed: ProxyRegistry.is_registered('rollout.update_weights') returns True
- This completes the Inference P2 series (GenerateProxy and UpdateWeightsProxy)
- Next priorities: 12.1-tool-call-proxy (Agent P2), 13.x-megatron/optimizer proxies, 14.x-data collection

[2026-01-24] 12.1-tool-call-proxy: COMPLETED
- Created ralph/proxies/agent.py with CallToolProxy class
- Inherits from BaseProxy, DelayMixin, ExceptionMixin
- Set SUPPORTED_STRATEGIES = {DELAY, TOOL_TIMEOUT, TOOL_EXCEPTION, WRONG_RESULT, EMPTY_RESULT, MALFORMED_RESULT}
- Implemented _get_layer() returning 'Agent'
- Implemented strategy methods:
  - _strategy_delay: Standard delay before tool call (default 10.0s)
  - _strategy_tool_timeout: Very long delay simulating hung tool (default 300.0s)
  - _strategy_tool_exception: Raises configurable exception with tool-specific error message
  - _strategy_wrong_result: Returns incorrect result with multiple corruption types:
    - negate: Inverts booleans, negates numbers, reverses strings
    - invert: Reverses collections, inverts dict key-values
    - randomize: Generates random values of same type
    - swap_type: Changes type (bool->int, dict->list, etc.)
  - _strategy_empty_result: Returns empty/None with type matching:
    - Supports none, empty_string, empty_dict, empty_list, zero, false
    - Optional call_original to detect and match original return type
  - _strategy_malformed_result: Returns structurally invalid result:
    - truncate: Shortens strings/lists/dicts by truncate_ratio
    - add_noise_keys: Adds random keys to dicts
    - wrap_extra: Wraps in extra nesting layers
    - corrupt_structure: Removes keys, adds invalid keys
- Helper methods:
  - _extract_tool_name: Extracts tool name from args/kwargs for error messages
  - _negate_result, _invert_result, _randomize_result, _swap_type_result: Result corruption
  - _get_empty_value, _get_empty_value_like: Empty value generation
  - _truncate_result, _add_noise_keys, _wrap_extra, _corrupt_structure: Malformation
- Decorated with @ProxyRegistry.register('ToolAgentLoop._call_tool')
- Updated ralph/proxies/__init__.py to export CallToolProxy
- Created tests/ralph/test_agent_proxy.py with 50+ comprehensive unit tests:
  - TestCallToolProxyRegistration: Registration and supported strategies
  - TestCallToolProxyBasics: _get_layer, no config, disabled config, unsupported strategy
  - TestDelayStrategy: Config usage, default delay
  - TestToolTimeoutStrategy: Config usage, default timeout
  - TestToolExceptionStrategy: Various types, default message, missing exc_type, tool name extraction
  - TestWrongResultStrategy: Specific value, negate, invert, swap_type, randomize, default swap_type
  - TestEmptyResultStrategy: None, empty_string, empty_dict, empty_list, zero, call_original, type matching
  - TestMalformedResultStrategy: truncate, add_noise_keys, wrap_extra, corrupt_structure, default
  - TestCallToolProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, args/kwargs passing
- All 8 manual tests pass
- Verification passed: `from ralph.proxies import CallToolProxy` works correctly
- Verification passed: ProxyRegistry.is_registered('ToolAgentLoop._call_tool') returns True
- This unblocks: 12.2-tool-parser-proxy (Agent P2)
- Next priorities: 12.2-tool-parser-proxy, 13.x-megatron/optimizer proxies, 14.x-data collection

[2026-01-24] 12.2-tool-parser-proxy: COMPLETED
- Added ToolParserProxy class to ralph/proxies/agent.py
- Inherits from BaseProxy, DelayMixin
- Set SUPPORTED_STRATEGIES = {PARSE_FAILURE, WRONG_TOOL_NAME, WRONG_ARGUMENTS, EXTRA_TOOL_CALLS, MISSING_TOOL_CALLS}
- Implemented _get_layer() returning 'Agent'
- Implemented strategy methods:
  - _strategy_parse_failure: Raises parse errors (syntax, json, timeout, encoding, schema)
    - Supports error_type parameter: "syntax", "json", "schema", "timeout", "encoding"
    - Supports custom error_message parameter
    - Raises appropriate exception type (ValueError, JSONDecodeError, TimeoutError, UnicodeDecodeError)
  - _strategy_wrong_tool_name: Returns incorrect tool names from parsing
    - Supports replacement_name for specific replacement
    - Supports corruption_type: "replace", "prefix", "suffix", "typo", "similar"
    - Handles list of tool calls, dict, and objects with name attribute
  - _strategy_wrong_arguments: Returns corrupted arguments
    - Supports corruption_type: "drop", "add_extra", "wrong_type", "wrong_value", "shuffle"
    - Handles JSON string arguments (parses, corrupts, re-serializes)
    - Supports drop_ratio and add_noise parameters
  - _strategy_extra_tool_calls: Adds spurious tool calls to parsing result
    - Supports extra_count parameter (default 2)
    - Supports position: "before", "after", "random"
    - Supports custom extra_tools list
    - Handles single dict, list, tuple, and None results
  - _strategy_missing_tool_calls: Removes some tool calls from result
    - Supports drop_ratio parameter (default 0.5)
    - Supports keep_first and keep_last parameters
    - Supports max_keep limit
    - Handles single dict, list, tuple, and None results
- Helper methods implemented:
  - _introduce_typo: Creates realistic typos (swap, delete, insert, replace with nearby key)
  - _get_similar_name: Maps get->fetch, set->update, etc.
  - _apply_to_tool_names: Recursively transforms tool names in various formats
  - _corrupt_arg_value: Handles various corruption types on arguments
  - _swap_argument_type: Changes types (bool->str, int->str, etc.)
  - _corrupt_argument_value: Changes values while preserving types
  - _generate_extra_tool_calls: Creates realistic-looking fake tool calls
  - _add_extra_tool_calls: Adds extras at specified position
  - _remove_tool_calls: Drops calls respecting keep_first/keep_last/max_keep
- Decorated with @ProxyRegistry.register('ToolParser.parse')
- Updated ralph/proxies/__init__.py to export ToolParserProxy
- Created 50+ comprehensive unit tests in tests/ralph/test_agent_proxy.py:
  - TestToolParserProxyRegistration: Registration and supported strategies
  - TestToolParserProxyBasics: _get_layer, no config, disabled config, unsupported strategy
  - TestParseFailureStrategy: Various error types, custom messages
  - TestWrongToolNameStrategy: Replacement, typo, prefix, suffix, similar, list handling
  - TestWrongArgumentsStrategy: Drop, add_extra, wrong_type, wrong_value, shuffle, JSON
  - TestExtraToolCallsStrategy: Count, position, custom tools, list input
  - TestMissingToolCallsStrategy: Drop ratio, keep_first, keep_last, max_keep, single dict
  - TestToolParserProxyIntegration: Step-based trigger, periodic trigger, collector recording, failure recording, args/kwargs passing
- All syntax verified via py_compile
- All 8 manual tests pass
- Verification passed: `from ralph.proxies import ToolParserProxy` works correctly
- Verification passed: ProxyRegistry.is_registered('ToolParser.parse') returns True
- This completes the Agent P2 series (CallToolProxy and ToolParserProxy)
- Next priorities: 13.1-megatron-optimizer-proxy, 13.3-optimizer-step-proxy, 14.1-log-parser
